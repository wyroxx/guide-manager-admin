services:
  admin:
    build:
      context: .
    ports:
      - "3000:3000"
      - "8000:8000"
    environment:
      FIREBASE_CREDENTIALS: /run/secrets/firebase.json
    volumes:
      - ${FIREBASE_CREDENTIALS:?Set FIREBASE_CREDENTIALS in .env}:/run/secrets/firebase.json:ro

